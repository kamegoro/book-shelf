import Box from '@/components/mui/Box';
import Typography from '@/components/mui/Typography';

export default function Home() {
  return (
    <Box
      sx={{
        height: '100vh',
      }}
    >
      <Typography sx={{ color: 'black', fontSize: 20 }}>HOME</Typography>
    </Box>
  );
}
